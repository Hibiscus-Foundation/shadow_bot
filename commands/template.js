const Discord = require("discord.js");

module.exports.run = async(bot, message, args) => {
    console.log("works") //Replace command here
}

module.exports.help = {
    name: "cmd" //replace with call
}